name: Repository Dispatch
on:
  repository_dispatch:
    types: [release-leafygreen-ui]
jobs:
  myEvent:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          ref: ${{ github.event.client_payload.ref }}
      - run: yarn update-packages ${{ github.event.client_payload }}